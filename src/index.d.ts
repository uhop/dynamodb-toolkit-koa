/**
 * Frozen re-export thunk — superseded by the `dynamodb-toolkit/koa`
 * subpath export (dynamodb-toolkit 3.8.0+). New code should import
 * `dynamodb-toolkit/koa` directly; the API is identical.
 */
export * from 'dynamodb-toolkit/koa';
